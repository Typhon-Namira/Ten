version: "1.0.0"
enabled: true
versions: {schema_version: "1.0", normalization_version: "1.0.0"}
providers:
  - {name: disabled, mode: disabled, enabled: false, version: "1"}
provider_priority: [disabled]
repository_mode: memory
